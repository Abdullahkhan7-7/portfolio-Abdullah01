<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Abdullah Akram | Software Engineer</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #0a0a0a;
            --accent-color: #c9a050; /* Luxury Gold */
            --text-main: #ffffff;
            --text-dim: #a0a0a0;
            --card-bg: #161616;
        }
 body {
    margin: 0;
    padding: 0;
    /* Background Image Link */
    background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
                url('https://images.unsplash.com/photo-1542831371-29b0f74f9713?auto=format&fit=crop&q=80&w=2070');
    
    background-size: cover; /* Puri screen pe fit aayegi */
    background-position: center; /* Image center mein rahegi */
    background-attachment: fixed; /* Scroll karne par pic nahi hilegi, premium lagega */
    background-repeat: repeat;
    color: white;
    height: 100vh;
}

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- Hero Section --- */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
            border-bottom: 1px solid #222;
        }

        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3rem, 8vw, 6rem);
            margin-bottom: 10px;
            letter-spacing: -2px;
        }

        .hero p {
            color: var(--accent-color);
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 4px;
            font-weight: 300;
        }

        /* --- Section Styling --- */
        section {
            padding: 100px 10%;
        }

        h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            margin-bottom: 40px;
            border-left: 4px solid var(--accent-color);
            padding-left: 20px;
        }

        /* --- Skills Grid --- */
        .skills-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .skill-card {
            background: var(--card-bg);
            padding: 30px;
            border-radius: 8px;
            transition: transform 0.3s ease;
            border: 1px solid #222;
        }

        .skill-card:hover {
            transform: translateY(-10px);
            border-color: var(--accent-color);
        }

        .skill-card h3 {
            color: var(--accent-color);
            margin-bottom: 15px;
        }

        .skill-tag {
            display: inline-block;
            background: #222;
            padding: 5px 12px;
            margin: 5px;
            border-radius: 4px;
            font-size: 0.9rem;
        }
        /* --- Animated Background --- */
        #particle-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -1; /* Keeps it behind all your content */
            pointer-events: none; /* Ensures you can still click links/forms */
            background-color: var(--bg-color); /* Uses your existing dark background */
        }
        
        /* Optional: Add a subtle fade-in to the whole body so the text appears over the particles smoothly */
        body {
            animation: fadeIn 1.5s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* --- Footer --- */
        footer {
            text-align: center;
            padding: 50px;
            color: var(--text-dim);
            font-size: 0.9rem;
            border-top: 1px solid #222;
        }

        footer strong {
            color: var(--accent-color);
        }
        <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Abdullah Akram | Pro Portfolio</title>
    <style>
        /* --- Luxury Animated Background --- */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: white;
            background: linear-gradient(-45deg, #0a0a0a, #1a1a1a, #2c2416, #000000);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            height: 100vh;
            overflow-x: hidden;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* --- Glassmorphism Effect for Content --- */
        .container {
            max-width: 900px;
            margin: 50px auto;
            padding: 40px;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            text-align: center;
        }

        h1 {
            font-size: 3.5rem;
            color: #c9a050; /* Gold */
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 5px;
        }

        .subtitle {
            font-size: 1.2rem;
            color: #a0a0a0;
            margin-bottom: 30px;
        }

        .project-box {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 40px;
        }

        .card {
            background: rgba(0, 0, 0, 0.3);
            padding: 20px;
            border-radius: 10px;
            border-bottom: 3px solid #c9a050;
            transition: 0.3s;
        }

        .card:hover {
            transform: scale(1.05);
            background: rgba(201, 160, 80, 0.1);
        }

        /* --- Luxury Form --- */
        input, textarea {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            background: rgba(0, 0, 0, 0.5);
            border: 1px solid #444;
            color: white;
            border-radius: 5px;
        }

        .btn {
            background: #c9a050;
            color: black;
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 10px;
        }
        /* Navbar Styling */
.navbar {
    position: fixed;
    top: 0;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 10%;
    background: rgba(10, 10, 10, 0.9); /* Halka sa transparent black */
    backdrop-filter: blur(10px); /* Glass look */
    z-index: 1000;
    border-bottom: 1px solid rgba(201, 160, 80, 0.2);
}

.logo {
    font-family: 'Playfair Display', serif;
    font-size: 1.8rem;
    font-weight: bold;
    color: #fff;
}

.logo span {
    color: #c9a050; /* Gold point */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 30px;
}

.nav-links a {
    text-decoration: none;
    color: #fff;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    transition: 0.3s;
    font-weight: 500;
}

.nav-links a:hover {
    color: #c9a050;
}

/* Smooth Scrolling effect */
html {
    scroll-behavior: smooth;
}

/* Page content ko thora niche karne ke liye kyunki navbar fixed hai */
section {
    padding-top: 120px !important;
}
    </style>
</head>
<body>
<nav class="navbar">
    <div class="logo">AA<span>.</span></div>
    <ul class="nav-links">
        <li><a href="#home">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#contact">Contact</a></li>
    </ul>
</nav>
    <div class="container">
    </style>
</head>
<body>
  

    <section class="hero">
        <p>Software Engineer</p>
        <h1>Abdullah Akram</h1>
        <p style="font-size: 1rem; color: var(--text-dim); margin-top: 20px;">Python Developer | Problem Solver</p>
    </section>

    <section id="about">
        <h2>About Me</h2>
        <p style="max-width: 800px; font-size: 1.1rem; color: var(--text-dim);">
            I am a dedicated Software Engineering student with a passion for building scalable back-end systems and clean user interfaces. 
            Focused on mastering the art of efficient coding and structural design.
        </p>
    </section>

    <section id="skills">
        <h2>Technical Arsenal</h2>
        <div class="skills-container">
            <div class="skill-card">
                <h3>Languages</h3>
                <span class="skill-tag">Python</span>
                <span class="skill-tag">C++</span>
                <span class="skill-tag">C</span>
                <span class="skill-tag">HTML</span>
                <span class="skill-tag">CSS</span>
                <span class="skill-tag">JavaScript</span>
            </div>
            <div class="skill-card">
                <h3>Core Concepts</h3>
                <span class="skill-tag">OOPs</span>
                <span class="skill-tag">Data Structures</span>
                <span class="skill-tag">Algorithms</span>
            </div>
            <div class="skill-card">
                <h3>Tools & Platforms</h3>
                <span class="skill-tag">VS Code</span>
                <span class="skill-tag">GitHub</span>
                <span class="skill-tag">Git</span>
                <span class="skill-tag">Linux</span>
            </div>
      

    <style>
        .contact-container {
            max-width: 600px;
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group input, .form-group textarea {
            width: 100%;
            padding: 15px;
            background: var(--card-bg);
            border: 1px solid #222;
            color: var(--text-main);
            border-radius: 4px;
            font-family: 'Inter', sans-serif;
            outline: none;
            transition: border-color 0.3s;
        }

        .form-group input:focus, .form-group textarea:focus {
            border-color: var(--accent-color);
        }

        .submit-btn {
            background: var(--accent-color);
            color: #000;
            padding: 15px 30px;
            border: none;
            border-radius: 4px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            transition: opacity 0.3s;
            align-self: flex-start;
        }

        .submit-btn:hover {
            opacity: 0.8;
        }

        #contact {
            background-color: #0f0f0f; /* Slightly different shade for depth */
        }
    </style>

        </div>
        <section id="projects">
        <h2>Featured Projects</h2>
        <div class="projects-grid">
            
            <div class="project-card">
                <span class="project-date">DEC 2024 — PRESENT</span>
                <h3>Custom WordPress Solutions</h3>
                <p>
                    Designing and developing fully responsive, high-quality WordPress websites. 
                    Focused on understanding client requirements to deliver reliable, outstanding solutions, 
                    whether building a dream website from scratch or seamlessly redesigning an existing platform for mobile, tablet, and desktop.
                </p>
                <div class="project-tags">
                    <span class="skill-tag">WordPress</span>
                    <span class="skill-tag">Responsive Design</span>
                    <span class="skill-tag">Web Development</span>
                </div>
            </div>

            <div class="project-card">
                <span class="project-date">ACADEMIC PROJECT</span>
                <h3>Automatic Gas Detector</h3>
                <p>
                    An automated hardware and software system engineered to ensure safety by detecting gas leakages. 
                    The system utilizes a specialized gas sensor to monitor the environment and instantly triggers an automated alarm system upon detecting hazardous levels.
                </p>
                <div class="project-tags">
                    <span class="skill-tag">Hardware Integration</span>
                    <span class="skill-tag">Sensors</span>
                    <span class="skill-tag">C++ / C</span>
                </div>
            </div>

            <div class="project-card">
                <span class="project-date">CLIENT PROJECT</span>
                <h3>Little Libas Storefront</h3>
                <p>
                    A fully responsive e-commerce storefront designed for a clothing brand. 
                    The platform features a dynamic product catalog, a seamless cart system, and an optimized user interface to drive sales and enhance the shopping experience.
                </p>
                <div class="project-tags">
                    <span class="skill-tag">Shopify</span>
                    <span class="skill-tag">Liquid</span>
                    <span class="skill-tag">CSS</span>
                    <span class="skill-tag">E-Commerce</span>
                </div>
            </div>

        </div>
              <section id="contact">
        <h2>Get In Touch</h2>
        <div class="contact-container">
            <p style="margin-bottom: 30px; color: var(--text-dim);">Have a project in mind or just want to say hi? Feel free to reach out.</p>
            
            <form action="https://formspree.io/f/abdullahkhanak776655@gmail.com" method="POST" class="contact-form">
                <div class="form-group">
                    <input type="text" name="name" placeholder="Your Name" required>
                </div>
                <div class="form-group">
                    <input type="email" name="_replyto" placeholder="Your Email" required>
                </div>
                <div class="form-group">
                    <textarea name="message" rows="5" placeholder="Your Message" required></textarea>
                </div>
                <button type="submit" class="submit-btn">Send Message</button>
            </form>
        </div>
    </section>
    </section>

    <style>
        /* --- Projects Grid --- */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
        }

        .project-card {
            background: var(--card-bg);
            padding: 40px;
            border-radius: 8px;
            border: 1px solid #222;
            transition: all 0.4s ease;
            display: flex;
            flex-direction: column;
        }

        .project-card:hover {
            transform: translateY(-10px);
            border-color: var(--accent-color);
            box-shadow: 0 10px 30px rgba(201, 160, 80, 0.05); /* Very subtle gold glow */
        }

        .project-date {
            font-size: 0.8rem;
            color: var(--accent-color);
            letter-spacing: 3px;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .project-card h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: var(--text-main);
        }

        .project-card p {
            color: var(--text-dim);
            font-size: 1rem;
            margin-bottom: 30px;
            flex-grow: 1; /* Pushes the tags to the bottom */
        }

        .project-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: auto;
        }
    </style>
    </section>

    <footer>
        <p>&copy; 2026 <strong>Abdullah Akram</strong>. Designed for Excellence.</p>
    </footer>

</body>
</html>